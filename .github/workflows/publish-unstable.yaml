name: '🚀 Publish (Unstable) Plugin'

on:
  push:
    branches:
      - unstable
  workflow_dispatch:

jobs:
  call:
    uses: jellyfin/jellyfin-meta-plugins/.github/workflows/publish-unstable.yaml@672e46d3ec5096731edbb7581ca872f3d8175cf8 # master
    secrets:
      deploy-host: ${{ secrets.REPO_HOST }}
      deploy-user: ${{ secrets.REPO_USER }}
      deploy-key: ${{ secrets.REPO_KEY }}
      token: ${{ secrets.JF_BOT_TOKEN }}
